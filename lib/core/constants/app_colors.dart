import 'package:flutter/material.dart';

class AppColors {
  static Color primaryGreen = const Color(
    0xFF0A683D,
  ); // Dark green from the button
  static Color lightGreyBackground = const Color.fromARGB(
    255,
    241,
    241,
    241,
  ); // Light grey for text fields
  static Color darkText = const Color(0xFF333333); // Dark text color
  static Color linkBlue = const Color(0xFF4285F4); // Blue for links
}
